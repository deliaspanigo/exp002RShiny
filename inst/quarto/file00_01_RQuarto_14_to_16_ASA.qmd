## Section 14 - Data Analysis - Part 01 of 03
Tomaremos los valores p del test de normalidad de Shapiro-Wilk para los residuos, el valor p del test de homogeneidad de varianza de Bartlett para los residuos, el valor p del test de Anova y la cantidad de grupos estadísticos detectados por el test de Tukey.
Con esta información y el valor alfa armaremos una tabla resumen general del lo que ha ocurrido en el análisis de datos. Esta tabla no representa la interpretación final del test, pero es un buen comienzo.

```{=html}
<script>
document.addEventListener('DOMContentLoaded', function(){
  // Seleccionar SOLO el panel con ID "mi-z99"
  const panel = document.getElementById('mi-z99');
  if (!panel) return;

  let tabs = panel.querySelectorAll('.nav a, .nav-link, [role="tab"]');

  // Activar SOLO la segunda pestaña de este panel
  if (tabs && tabs.length > 1) {
    tabs[1].click();
  }
});
</script>
```

::: {.panel-tabset #mi-z99}
### R Code

```{r eval=TRUE, echo=TRUE}

  # From Shapiro test ----------------------------------------------------------
  p_value_normality_internal <- list_test_residuals_normality$"p.value"
  
  check_normality_ho_rejected  <- p_value_normality_internal < alpha_value
  status_normality_ho   <- ifelse(test = check_normality_ho_rejected, 
                                   yes = "Ho Rejected", 
                                    no = "Ho no rejected")
  
  AQ_normality_rejected_ho <- ifelse(test = check_normality_ho_rejected, 
                                      yes = "Yes", 
                                       no = "No")
    
  AQ_normality_is_valid <- "Yes, it is."

  p_value_normality_external <- ifelse(test = p_value_normality_internal < 0.01, 
                                        yes = "<<0.01", 
                                         no = as.character(p_value_normality_internal))
  # From bartlett test ---------------------------------------------------------
  p_value_homogeneity_internal  <- list_test_residuals_homogeneity$"p.value"
  check_homogeneity_ho_rejected  <- p_value_homogeneity_internal < alpha_value
  status_homogeneity_ho <- ifelse(test = check_homogeneity_ho_rejected, 
                                   yes = "Ho Rejected", 
                                    no = "Ho no rejected")
  
  AQ_homogeneity_rejected_ho <- ifelse(test = check_homogeneity_ho_rejected, 
                                        yes = "Yes", 
                                         no = "No")
    
  AQ_homogeneity_is_valid <- "Yes, it is."

  p_value_homogeneity_external <- ifelse(test = p_value_normality_external < 0.01, 
                                          yes = "<<0.01", 
                                           no = as.character(p_value_normality_external))
  
  # Check for all requeriments -------------------------------------------------
  check_ok_all_requeriments     <- sum(check_normality_ho_rejected, check_homogeneity_ho_rejected) == 0
  AQ_all_requeriments_ok <- ifelse(test = check_ok_all_requeriments, 
                                    yes = "Yes", 
                                     no = "No")

  # Anova ----------------------------------------------------------------------
  p_value_anova_internal <- df_table_anova$"Pr(>F)"[1]
  check_anova_ho_rejected <-   p_value_anova_internal < alpha_value
  status_anova_ho <- ifelse(test = check_anova_ho_rejected, 
                             yes = "Ho Rejected", 
                              no = "Ho no rejected")
  
  AQ_anova_rejected_ho <- ifelse(test = check_anova_ho_rejected, 
                                  yes = "Yes", 
                                   no = "No")
    
  AQ_anova_is_valid <- ifelse(test = check_ok_all_requeriments, 
                               yes = "Yes, it is.", 
                                no = "No, is not!!!")
  
  p_value_anova_external <- ifelse(test = p_value_anova_internal < 0.01, 
                                    yes = "<<0.01", 
                                     no = as.character(p_value_anova_internal))
  
  # Tukey ----------------------------------------------------------------------
  amount_groups_tukey <- length(unique(tukey01_full_groups$groups))
  check_tukey_groups      <- amount_groups_tukey >= 2
  status_tukey <- ifelse(test = check_tukey_groups, 
                          yes = "At least two groups of means.", 
                           no = "Only one group.")
  
  AQ_tukey_groups <- ifelse(test = check_tukey_groups, 
                             yes = "Yes", 
                              no = "No")
    
  AQ_tukey_is_valid <- ifelse(test = check_ok_all_requeriments && check_anova_ho_rejected, 
                               yes = "Yes, it is.", 
                                no = "No, is not!!!")

  # Anova and Tukey ----------------------------------------------------------------------
  check_anova_tukey_coherence      <- check_anova_ho_rejected == check_tukey_groups
  check_anova_yes_tukey_no <- check_anova_ho_rejected  & (!check_tukey_groups)
  check_anova_no_tukey_yes <- !check_anova_yes_tukey_no

  # DF summary ----------------------------------------------------------------------
  vector_p_value_internal <- c(p_value_normality_internal, p_value_homogeneity_internal, p_value_anova_internal)
  vector_p_value_external <- c(p_value_normality_external, p_value_homogeneity_external, p_value_anova_external)
  vector_logic_rejected <- vector_p_value_internal < alpha_value
  vector_ho_decision <- ifelse(test = vector_logic_rejected, yes = "Ho Rejected", "Ho no rejected")
  vector_ho_rejected <- ifelse(test = vector_logic_rejected, yes = "Yes", "No")
  
  df_summary_anova <- data.frame(
    "name"        = c("Shapiro-Wilk test"       , "Bartlett test"             , "Anova 1 way"         , "Tukey"),
    "test"        = c("Normality"               , "Homogeneity"               , "Analysis of Variance", "Mean groups"),
    "variable"    = c("residuals"               , "residuals"                 , var_name_rv           , var_name_rv),
    "p_value"     = c(p_value_normality_internal, p_value_homogeneity_internal, p_value_anova_internal, NA),
    "alpha_value" = c(alpha_value               , alpha_value                 , alpha_value           , alpha_value),
    "Decision"    = c(status_normality_ho       , status_homogeneity_ho       , status_anova_ho       , status_tukey),
    "Valid"       = c(AQ_normality_is_valid     , AQ_homogeneity_is_valid     , AQ_anova_is_valid     , AQ_tukey_is_valid)
  )
  
  df_summary_anova
```

### Table
Como es una tabla particularmente importante, nos daremos el lujo de detallarla también de una hermosa manera.
```{r eval=TRUE, echo=FALSE}

knitr::kable(df_summary_anova, 
             align = c('l', 'l', 'c', 'r', 'r', 'c', 'c'))
```
:::


## Section 15 - Data Analysis - Part 02 of 03
Hasta el momento tenemos una tabla resumen, ordenada con información general de la interpretación del test.  
Generamos ahora la primera tanda de frases automáticas explicativas para cada test por separado.




```{=html}
<script>
document.addEventListener('DOMContentLoaded', function(){
  // Seleccionar SOLO el panel con ID "mi-z02"
  const panel = document.getElementById('mi-z02');
  if (!panel) return;

  let tabs = panel.querySelectorAll('.nav a, .nav-link, [role="tab"]');

  // Activar SOLO la segunda pestaña de este panel
  if (tabs && tabs.length > 1) {
    tabs[1].click();
  }
});
</script>
```

::: {.panel-tabset #mi-z02}
####  R Code

##### Normality test for residuals
Armamos una frase mínima con dos casos posibles.
```{r eval=TRUE, echo=TRUE}
  phrase01_normality_yes_rejected <- "
The null hypothesis of normal distribution of residuals is rejected.
"

  phrase01_normality_no_rejected  <- "
The null hypothesis of normal distribution of residuals is not rejected.
"
  phrase01_normality_selected     <- ifelse(test = check_normality_ho_rejected, 
                                           yes = phrase01_normality_yes_rejected, 
                                            no = phrase01_normality_no_rejected)
  
  cat(phrase01_normality_selected)
```
  
Armamos una segunda frase con una explicación más detallada.
```{r eval=TRUE, echo=TRUE}
phrase02_normality_yes_rejected <- "
The p value about normality distibution is _p_value_.  
Alpha value is _alpha_value_.  
The p value is less than alpha value.  
The null hypothesis of normal distribution of residuals is rejected.  
Los residuos no poseen distribución normal.
"

phrase02_normality_no_rejected  <- "
The p value about normality distibution is _p_value_.  
Alpha value is _alpha_value_.  
The p value is equal or mayor than alpha value.  
The null hypothesis of normal distribution of residuals is not rejected.  
Los residuos poseen distribución normal.
"

phrase02_normality_selected     <- ifelse(test = check_normality_ho_rejected, 
                                        yes = phrase02_normality_yes_rejected, 
                                        no = phrase02_normality_no_rejected)

replacements_normality <- c(
  "_p_value_" = as.character(p_value_normality_internal),
  "_alpha_value_" = as.character(alpha_value)
)

# 2. Realizar el reemplazo múltiple
phrase02_normality_selected <- str_replace_all(
  string = phrase02_normality_selected,
  pattern = replacements_normality
)

cat(phrase02_normality_selected)

```

##### Homogeneity test for residuals

```{r eval=TRUE, echo=TRUE}
  phrase01_homogeneity_yes_rejected <- "
The hypothesis of homogeneity of variances (homoscedasticity) is rejected.
"
  phrase01_homogeneity_no_rejected  <- "
  The hypothesis of homogeneity of variances (homoscedasticity) is not rejected.
"
  
  phrase01_homogeneity_selected     <- ifelse(test = check_homogeneity_ho_rejected, 
                                         yes = phrase01_homogeneity_yes_rejected, 
                                         no = phrase01_homogeneity_no_rejected)
  
  cat(phrase01_homogeneity_selected)
```

Definimos una segunda frase con una explicación más detallada.
```{r eval=TRUE, echo=TRUE}
  phrase02_homogeneity_yes_rejected <- "
The p value about homogeneity variances from residuals is _p_value_.  
Alpha value is _alpha_value_.  
The p value is less than alpha value.  
The null hypothesis of homogeneity of variances from residuals is rejected.  
Los residuos son homogeneos.
"

  phrase02_homogeneity_no_rejected  <- "
The p value about normality distibution is _p_value_.  
Alpha value is _alpha_value_.  
The p value is equal or mayor than alpha value.  
The null hypothesis of normal distribution of residuals is not rejected.  
Los residuos son homogeneos."

  phrase02_homogeneity_selected     <- ifelse(test = check_homogeneity_ho_rejected, 
                                         yes = phrase02_homogeneity_yes_rejected, 
                                         no = phrase02_homogeneity_no_rejected)


replacements_homogeneity <- c(
  "_p_value_" = as.character(p_value_homogeneity_internal),
  "_alpha_value_" = as.character(alpha_value)
)

# 2. Realizar el reemplazo múltiple
phrase02_homogeneity_selected <- str_replace_all(
  string = phrase02_homogeneity_selected,
  pattern = replacements_homogeneity
)

  cat(phrase02_homogeneity_selected)
```

##### All requeriments
Determinamos una frase corta para explicar lo que ocurre con los requisitos del modelo.
```{r eval=TRUE, echo=TRUE} 
  phrase01A_requeriments_yes_valid <- "
All requirements for the model are met.
"
  phrase01A_requeriments_no_valid  <- "
Not all requirements for the model are met.
"
  
  phrase01A_requeriments_selected  <- ifelse(test = check_ok_all_requeriments, 
                                          yes = phrase01A_requeriments_yes_valid, 
                                          no = phrase01A_requeriments_no_valid)
  
  cat(phrase01A_requeriments_selected)  
```  

```{r eval=TRUE, echo=TRUE} 
  phrase01B_requeriments_yes_valid <- "
It is valid to draw conclusions from the ANOVA test.
"
  phrase01B_requeriments_no_valid  <- "
It is NOT valid to draw conclusions from the ANOVA test.
"
  
  phrase01B_requeriments_selected  <- ifelse(test = check_ok_all_requeriments, 
                                          yes = phrase01B_requeriments_yes_valid, 
                                          no = phrase01B_requeriments_no_valid)
  
  cat(phrase01B_requeriments_selected)  
```  

```{r eval=TRUE, echo=TRUE} 
  phrase01C_requeriments_yes_valid <- "
Se prosigue con el análisis.
"
  phrase01C_requeriments_no_valid  <- "
Al no cumplirse los requisitos del modelo debe descartarse el análisis de Anova y Tukey
indistintamente de los valores estadísticos obtenidos. Se detalla a continuación la 
interpretación literal y descontextualizada del test de Anova y el test de Tukey solo 
a fines demostrativos, pero sin ninguna validez para obtener conclusiones a partir de ellos.
"
  
  phrase01C_requeriments_selected  <- ifelse(test = check_ok_all_requeriments, 
                                          yes = phrase01C_requeriments_yes_valid, 
                                          no = phrase01C_requeriments_no_valid)
  
  cat(phrase01C_requeriments_selected)  
```  

Agregamos una frase larga, que detalla tambien otros temas de cada caso.
```{r eval=TRUE, echo=TRUE} 
  phrase02A_requeriments_yes_valid <- phrase01A_requeriments_yes_valid

  phrase02A_requeriments_no_valid  <- phrase01A_requeriments_no_valid
  
  
  phrase02A_requeriments_selected  <- ifelse(test = check_ok_all_requeriments, 
                                          yes = phrase02A_requeriments_yes_valid, 
                                          no = phrase02A_requeriments_no_valid)
  
  
  cat(phrase02A_requeriments_selected)  
```  

Agregamos una frase larga, que detalla tambien otros temas de cada caso.
```{r eval=TRUE, echo=TRUE} 
  phrase02B_requeriments_yes_valid <- "
All requirements for the model are met.
"
  phrase02B_requeriments_no_valid  <- "
It is NOT valid to draw conclusions from the ANOVA test. Este set de datos debe ser analizado con otra herramienta estadística como podrían ser el test de Kruskal-Wallis.
Si se quiere una herramienta más sofisticada, podría evaluarse la posibilidad de utilizar modelos lineales
generales mixtos, modelos lineales generales, modelos lineales generalizados mixtos, modelos lineales de
distribución exacta, etc.
"
  phrase02B_requeriments_selected  <- ifelse(test = check_ok_all_requeriments, 
                                          yes = phrase02B_requeriments_yes_valid, 
                                          no = phrase02B_requeriments_no_valid)
  
  
  cat(phrase02B_requeriments_selected)  
```  

Armamos una frase más que servirá como aviso al usuario de que si los requisitos no se cumplen el resultado 
de Anova y Tukey no debe ser interpretado.
```{r eval=TRUE, echo=TRUE} 
  phrase02C_requeriments_yes_valid <- "
It is valid to draw conclusions from the ANOVA test
"
  phrase02C_requeriments_no_valid  <- "
It is NOT valid to draw conclusions from the ANOVA test.
Al no cumplirse los requisitos del modelo debe descartarse el análisis de Anova y Tukey
indistintamente de los valores estadísticos obtenidos. Se detalla a continuación la 
interpretación literal y descontextualizada del test de Anova y el test de Tukey solo 
a fines demostrativos, pero sin ninguna validez para obtener conclusiones a partir de ellos.
"
  
  phrase02C_requeriments_selected  <- ifelse(test = check_ok_all_requeriments, 
                                          yes = phrase02C_requeriments_yes_valid, 
                                          no = phrase02C_requeriments_no_valid)
  
  cat(phrase02C_requeriments_selected)  
```  

##### Anova test
Definimos si se rechaza o no H0 para el test de Anova.
Definimos una frase mínima con dos casos posibles.
```{r eval=TRUE, echo=TRUE} 

  phrase01_anova_yes_rejected <- "
The null hypothesis of the ANOVA test is rejected. 
There are statistically significant differences in at least one level of the factor.
"

  phrase01_anova_no_rejected  <- "
The null hypothesis of the ANOVA test is not rejected.
All levels of the factor are statistically equal.
"
  
  phrase01_anova_selected     <- ifelse(test = check_anova_ho_rejected, 
                                      yes = phrase01_anova_yes_rejected, 
                                      no = phrase01_anova_no_rejected)
  

 cat(phrase01_anova_selected)

```

Definimos una segunda frase con una explicación más detallada.
```{r eval=TRUE, echo=TRUE} 

  phrase02_anova_yes_rejected <- "
Para el test de anova el valor p de es _p_value_.  
El valor alpha es _alpha_value_.  
El valor p es menor al valor alpha.  
Se rechaza la hipotesis nula de igual de medias del test de Anova.  
There are statistically significant differences in at least one level of the factor.
Al rechazarse la hipotesis nula el test de Anova garantiza diferencias estadísticamente significativas 
al menos entre el nivel del factor con la media menor y el nivel del factor con la media mayor. 
"

  phrase02_anova_no_rejected  <- "
Para el test de Anova el valor p es _p_value_.  
El valor alpha es _alpha_value_.  
El valor p es mayor o igual al valor alpha.  
No se rechaza la hipotesis nula de igual de medias del test de Anova.  
There are not statistically significant differences entre los niveles del factor.  
Las diferencias observadas entre los niveles del factor han ocurrido por azar.
Todos los niveles del factor son estadísticamente iguales.  
"

  phrase02_anova_selected     <- ifelse(test = check_anova_ho_rejected, 
                                      yes = phrase02_anova_yes_rejected, 
                                      no = phrase02_anova_no_rejected)
  
replacements_anova <- c(
  "_p_value_" = as.character(p_value_anova_internal),
  "_alpha_value_" = as.character(alpha_value)
)

# 2. Realizar el reemplazo múltiple
phrase02_anova_selected <- str_replace_all(
  string = phrase02_anova_selected,
  pattern = replacements_anova
)

  cat(phrase02_anova_selected)

```


##### Tukey test
Definimos una frase mínima con dos casos posibles.
```{r eval=TRUE, echo=TRUE} 

  phrase01A_tukey_yes_groups <- "
Al ser rechazada la hipótesis nula de Anova, es válida la utilización de un test de comparaciones 
múltiple que acompañe al test de Anova.
"
  phrase01A_tukey_no_groups  <- "
Al no ser rechazada la hipótesis nula de Anova, no es válida la utilización de un test de comparaciones múltiple.
"
  phrase01A_tukey_selected     <- ifelse(test = check_anova_ho_rejected, 
                                      yes = phrase01A_tukey_yes_groups, 
                                      no = phrase01A_tukey_no_groups)
  

  cat(phrase01A_tukey_selected)

```

```{r eval=TRUE, echo=TRUE} 

  phrase01B_tukey_yes_groups <- "
Es seleccionado el test de Tukey como test de comparaciones múltiple para este script.
"
  phrase01B_tukey_no_groups  <- "
Se detalla a continuación la 
interpretación literal y descontextualizada del test de Tukey solo 
a fines demostrativos, pero sin ninguna validez para obtener conclusiones a partir de ellos.
"
  phrase01B_tukey_selected     <- ifelse(test = check_anova_ho_rejected, 
                                      yes = phrase01B_tukey_yes_groups, 
                                      no = phrase01B_tukey_no_groups)
  

  cat(phrase01B_tukey_selected)

```

```{r eval=TRUE, echo=TRUE} 

  phrase01C_tukey_yes_groups <- "
El test de Tukey indica que existen al menos 2 grupos entre los niveles del factor.
Cantidad de grupos y estructura debe ser analizada en detalle con la tabla de Tukey.
"
  phrase01C_tukey_no_groups  <- "
El test de Tukey indica que entre los niveles del factor existe un solo grupo estadístico.
"
  phrase01C_tukey_selected     <- ifelse(test = check_tukey_groups, 
                                      yes = phrase01C_tukey_yes_groups, 
                                      no = phrase01C_tukey_no_groups)
  

  cat(phrase01C_tukey_selected)

```


Definimos una segunda frase con una explicación más detallada.
```{r eval=TRUE, echo=TRUE} 

  phrase02A_tukey_yes_groups <- phrase01A_tukey_yes_groups

  phrase02A_tukey_no_groups  <- phrase01A_tukey_no_groups
  
  phrase02A_tukey_selected     <- ifelse(test = check_anova_ho_rejected, 
                                      yes = phrase02A_tukey_yes_groups, 
                                      no = phrase02A_tukey_no_groups)
  

  cat(phrase02A_tukey_selected)

```

```{r eval=TRUE, echo=TRUE} 

  phrase02B_tukey_yes_groups <- phrase01B_tukey_yes_groups

  phrase02B_tukey_no_groups  <- phrase01B_tukey_no_groups
  
  phrase02B_tukey_selected     <- ifelse(test = check_anova_ho_rejected, 
                                      yes = phrase02B_tukey_yes_groups, 
                                      no = phrase02B_tukey_no_groups)
  

  cat(phrase02B_tukey_selected)

```

```{r eval=TRUE, echo=TRUE} 

  phrase02C_tukey_yes_groups <- "
En el test de Tukey detalla al menos 2 grupos estadísticamente diferentes. 
Ahora debe considerarse la estructura de grupos detallada por el test de Tukey para poder dar una recomendación en su área de trabajo.
"

  phrase02C_tukey_no_groups  <- "
El test de Tukey detalla que todos los niveles del factor conforman un único grupo.
Desde el punto de vista de Tukey todos los niveles del factor son estadísticamente iguales.
"

  phrase02C_tukey_selected     <- ifelse(test = check_tukey_groups, 
                                      yes = phrase02C_tukey_yes_groups, 
                                      no = phrase02C_tukey_no_groups)
  
  
  cat(phrase02C_tukey_selected)

```

##### Anova and Tukey relation
Podría ocurrir que ambos análisis no sean coherentes entre si, en el sentido que uno detecte diferencias
entre los niveles dle factor y el otro no; o visceversa. Anova es un test más potente que cualquier 
test de comparación múltiple, por lo tanto siempre la última palabra respecto a los niveles del factor la tiene Anova.
Es por ello que para tener en cuenta este punto, generamos una frase en particular.
```{r eval=TRUE, echo=TRUE} 

  phrase01_anova_yes_tukey_no <- "
Anova indica que existen al menos 2 grupos mientras Tukey indica que hay solo un grupo estadśitico. 
Indistintamente de que Tukey no ha encontrado grupos, al rechazarse la hipótesis nula de anova, 
al menos puede afirmarse que existen diferencias estadísticamente significativas entre el nivel del 
factor con la media mayor y el nivel del factor con la media menor.
"

  phrase01_anova_no_tukey_yes  <- "
Anova indica que todos los niveles del factor son iguales mientras que Tukey a encontrado al menos 2 grupos.  
Tukey está supeditado al rechazo de la hipótesis de Anova.  
Al no rechazarse la hipótesis nula de anova el test de Tukey no debe ser tenido en cuenta para tomar decisiones.
Todos los niveles del factor son iguales.
"


phrase01_anova_tukey_selected     <- ifelse(test = check_anova_yes_tukey_no,                                                                                   yes = phrase01_anova_yes_tukey_no, 
                                              no = phrase01_anova_no_tukey_yes)
  

  cat(phrase01_anova_tukey_selected)

```

```{r eval=TRUE, echo=TRUE} 

  phrase02_anova_yes_tukey_no <- "
Indistintamente de que Tukey no ha encontrado grupos, al rechazarse la hipótesis nula de anova, 
al menos puede afirmarse que existen diferencias estadísticamente significativas entre el nivel del 
factor con la mayor media y el nivel del factor con la menor media.
"

  phrase02_anova_no_tukey_yes  <- "
Indistintamente de que Tukey a encontrado grupos, al no rechazarse la hipótesis nula de anova
todos los niveles del factor son iguales.
"


phrase02_anova_tukey_selected     <- ifelse(test = check_anova_yes_tukey_no,                                                                                   yes = phrase02_anova_yes_tukey_no, 
                                              no = phrase02_anova_no_tukey_yes)
  

  cat(phrase01_anova_tukey_selected)

```

Tenemos entonces ya todo un conjunto de salidas con frases cortas que se pueden usar en 
diferentes partes del reporte.
```{r eval=TRUE, echo=TRUE}
cat(phrase01_normality_selected, 
    phrase01_homogeneity_selected,
    phrase01A_requeriments_selected,
    phrase01B_requeriments_selected,
    phrase01C_requeriments_selected,
    phrase01_anova_selected,
    phrase01A_tukey_selected,
    phrase01B_tukey_selected,
    phrase01C_tukey_selected,
    phrase01_anova_tukey_selected)

```

Y un segundo conjunto conformado dpor frases más largas que sirven para acompaár a resultados estadísticos
o propiamente para armar un párrafo o una página del informe.

```{r eval=TRUE, echo=TRUE}
cat(phrase02_normality_selected, 
    phrase02_homogeneity_selected,
    phrase02A_requeriments_selected,
    phrase02B_requeriments_selected,
    phrase02C_requeriments_selected,
    phrase02_anova_selected,
    phrase02A_tukey_selected,
    phrase02B_tukey_selected,
    phrase02C_tukey_selected,
    phrase02_anova_tukey_selected)

```

####  Phrases
La primera frase es corta, y sirve para resumir. Sin detalles.
```{r eval=TRUE, echo=FALSE}
cat(phrase01_normality_selected, 
    phrase01_homogeneity_selected,
    phrase01A_requeriments_selected,
    phrase01B_requeriments_selected,
    phrase01C_requeriments_selected,
    phrase01_anova_selected,
    phrase01A_tukey_selected,
    phrase01B_tukey_selected,
    phrase01C_tukey_selected,
    phrase01_anova_tukey_selected)

```

La segunda frase es más larga y sirve para ayudar al operador (usted) a interpretar de manera apropiada el valor p obtenido para el test de normalidad de Shapiro-Wilk aplicado sobre los residuos.
```{r eval=TRUE, echo=FALSE}
cat(phrase02_normality_selected, 
    phrase02_homogeneity_selected,
    phrase02A_requeriments_selected,
    phrase02B_requeriments_selected,
    phrase02C_requeriments_selected,
    phrase02_anova_selected,
    phrase02A_tukey_selected,
    phrase02B_tukey_selected,
    phrase02C_tukey_selected,
    phrase02_anova_tukey_selected)

```
:::






## Section 16 - Data Analysis - Part 03 of 03
En las secciones anteriores hemos armado una tabla simple pero que logra resumir toda 
la información de Anova en sentido amplio.

En la parte 02 manifestamos todas decisiones de manera explícita y obtuvimos que ello
un conjunto de frases explciativas que por separado podrán acompañar a tablas y gráficos, y que juntan 
conforman ya un buen párrafo respecto a nuestros datos.

En esta 3ra parte iremos más allá y ordenaremos la forma en que piensa un asesor estadístico humano
a la hora de enfrentarse a un test de Anova y conformaremos un asesor estadístico automatizado. 
La idea será obtener aún más frases y explicaciones de los valores estadísticos obtenidos, pero 
con alguna explicación extra teórica cuando sea necesario y oportuno.

La sección siguiente se llama 'Orden y Progreso', y merece algunas palabras de introducción.  
El sentido central, directo y explícito de la sección 'Orden y Progreso' es quitar toda la niebla, magia, misterio y miedo alrededor de lo que conforma el análisis de datos para Anova. Quedará también en manifiesto que el orden 
y el progreso obtenidos no son por utilizar R ni cualquier otro software, sino que el orden y el progreso serán debido a la manifestación clara y explícita de los criterios netamente estadísticos, que brillan por su
ausencia tanto en los cursos de estadística y como en las publicaciones científicas modernas a la vez que polulan los script de trabajo.

Los criterios bien podrás ser aplicados en cualquier otro software o incluso realizando a mano los resultados. 
Al finalizar la sección esperamos que usted adquiera una visión mucho más perspicaz sobre como un 
asesor estadístico humano encara a la toma de decisiones, y lo ayude a navegar aguas que parecen inciertas con total certeza de lo que debe hacer.    
Dicho esto, comenzamos.  

### Orden y progreso
Para la toma de decisiones, debemos tener en cuenta 3 aspectos:  
- Si se cumplen o no todos los requisitos del modelo.  
- Si se rechaza o no Ho de Anova.  
- Si se encuentran o no al menos 2 grupos en el test de Tukey.  

Cada aspecto a tener en cuenta tiene 2 posibilidades, y son 3 aspectos en total.  
La teoría de combinatoria indica entonces que tenemos 2^3 = 8 combinaciones posibles.  
Cualquier conjunto de resultados estadístico de anova obtenido por cualquier medio cae en un caso, solo en un caso y nada más que en un caso, sin excepciones.  
Debido a que todas las combinaciones en donde no se cumplen los requisitos del modelo llevan al 
mismo destino es que estos 8 casos pueden resumirse directamente en 5 casos estadísticos.  

Lo que hemos hecho es pasar de no tener idea a qué nos podemos enfrentar cuando analizamos un test de Anova, a saber que solo existen 5 casos posibles y que cada uno de los casos puede ser perfectamente tipificado.  
Es completametne factible detallar 5 casos con todos sus detalles, y luego verificar en que caso hemos caído con nuestros datos.

```{=html}
<script>
document.addEventListener('DOMContentLoaded', function(){
  // Seleccionar SOLO el panel con ID "mi-z33"
  const panel = document.getElementById('mi-z33');
  if (!panel) return;

  let tabs = panel.querySelectorAll('.nav a, .nav-link, [role="tab"]');

  // Activar SOLO la segunda pestaña de este panel
  if (tabs && tabs.length > 1) {
    tabs[1].click();
  }
});
</script>
```

#### Tabla General de casos
::: {.panel-tabset #mi-z33}
##### R Code

```{r eval=TRUE, echo=TRUE}


df_decision_cases_init <- data.frame(
  "Statistic Case" =          c("Case 1"         , "Case 2"         , "Case 3"         , "Case 4"    , "Case 5"    ),
  "All requeriments OK" =     c(  "No"           ,   "Yes"          ,  "Yes"           , "Yes"       ,  "Yes"      ),
  "Anova rejected" =          c("No/Yes"         ,   "No"           ,  "No"            , "Yes"       ,  "Yes"      ), 
  "Tukey at least 2 groups" = c("No/Yes"         ,   "No"           ,  "Yes"           , "No"        ,  "Yes"      ),
  "Decision" =                c("Decision Case 1", "Decision Case 2", "Decision Case 3", "Decision Case 4", "Decision Case 5")
)

df_decision_cases_init
```

#####  Table
Como es una tabla particularmente importante, nos daremos el lujo de detallarla también de una hermosa manera.
```{r eval=TRUE, echo=FALSE}

knitr::kable(x = df_decision_cases_init, 
             caption = "", 
             align = rep("c", ncol(df_decision_cases_init)))



```
:::



```{=html}
<script>
document.addEventListener('DOMContentLoaded', function(){
  // Seleccionar SOLO el panel con ID "mi-z55"
  const panel = document.getElementById('mi-z55');
  if (!panel) return;

  let tabs = panel.querySelectorAll('.nav a, .nav-link, [role="tab"]');

  // Activar SOLO la segunda pestaña de este panel
  if (tabs && tabs.length > 1) {
    tabs[2].click();
  }
});
</script>
```

Tendremos esta estructura de casos para 4 temáticas en particular.  
Temática 1) Visión General y teoría estadística.  
Temática 2) Toma de decisiones, especificaciones y teoría estadística de los Requisitos.  
Temática 3) Toma de decisiones, especificaciones y teoría estadística del test de Anova.  
Temática 4) Toma de decisiones, especificaciones y teoría estadística de Tukey.  

Estos 4 temáticas serán detallados para los 5 casos estadísticos.
Serán detalladas las 4 temáticas para los 5 casos estadísticos como texto en objetos dataframe.
Son en total 4x5=20 frases explicativas que serán seleccionadas según el caso al cual nuestros datos correspondan.


#### El caso de nuestros datos
Tenemos ya el marco genereal de casos.  
Nosotros ya tenemos la información suficiente como para determinar en qué caso nos encontramos aunque aún no sabemos que consecuencias trae eso.  
Determinamos el caso de nuestros datos, y luego veremos las consecuencias.  


::: {.panel-tabset #mi-z55}
#####  R Code

Las siguientes sentencias lo que hacen es detectar nuestros resultados a que caso corresponde.
Primero ordenamos la información de nuestro análisis en el mismo orden que la tabla.
```{r eval=TRUE, echo = TRUE}
vector_observed_details <- c(AQ_all_requeriments_ok, AQ_anova_rejected_ho,  AQ_tukey_groups)
names(vector_observed_details) <- c("All_requeriments_ok", "anova_rejected_ho", "tukey_groups")
vector_observed_details
```

Ahora buscamos esas características en la tabla, y detallamos la fila:
```{r eval=TRUE, echo = TRUE}
selected_col <- 1:length(vector_observed_details) + 1

# 2. Realizar la comparación elemento por elemento
# Comparamos las columnas seleccionadas de df_decision_cases 
# con los elementos de vector_observed_details
match_matrix <- mapply(
  function(pattern, text) {
    grepl(pattern, text, fixed = TRUE)
  }, 
  pattern = vector_observed_details, 
  text = df_decision_cases_init[, selected_col]
)

# 3. Identificar la fila donde *todos* los elementos coinciden
# La coincidencia total se da cuando la suma de 'TRUE' (1) en una fila
# es igual al número total de características (length(selected_col)).
row_match_index <- which(rowSums(match_matrix) == length(selected_col))
row_match_index

```

#####  Full table cases
```{r eval=TRUE, echo=FALSE}

knitr::kable(df_decision_cases_init)%>%
    kableExtra::row_spec(
    row = row_match_index, # Usa el índice de la fila coincidente
    extra_css = "background-color: #FFFFCC;" # Color de fondo suave (amarillo claro)
    # Otros colores comunes: #E6F7FF (Azul claro), #E6FFE6 (Verde claro)
    )
```

#####  Selected Case
```{r eval=TRUE, echo=FALSE}
df_decision_cases_selected <- df_decision_cases_init[row_match_index, ]
knitr::kable(df_decision_cases_selected)%>%
    kableExtra::row_spec(
    row = 1, # Usa el índice de la fila coincidente
    extra_css = "background-color: #FFFFCC;" # Color de fondo suave (amarillo claro)
    # Otros colores comunes: #E6F7FF (Azul claro), #E6FFE6 (Verde claro)
    )
```
:::

Dentro de los casos posibles, nuestros resultados obtenidos se encuentran en 
el <strong style="font-size: 1.2em;">caso `r row_match_index`</strong>.  
Lo que haremos a continuación es completar toda la casuística asociada a cada parte del análisis para luego simplemente poder visualizar las interpretación de nuestro caso en particular.

### Asesor estadístico automatizado - Temática 1) Visión General
Comenzamos detallando la visión general y los aspectos que comunmente tiene en mente un asesor estadístico humano a la hora de analizar el test de Anova. Lo que se otorga es una visión global resumina con detalles teóricos.

```{=html}
<script>
document.addEventListener('DOMContentLoaded', function(){
  // Seleccionar SOLO el panel con ID "mi-tabset"
  const panel = document.getElementById('mi-tabset');
  if (!panel) return;

  let tabs = panel.querySelectorAll('.nav a, .nav-link, [role="tab"]');

  // Activar SOLO la segunda pestaña de este panel
  if (tabs && tabs.length > 1) {
    tabs[3].click();
  }
});
</script>
```

::: {.panel-tabset #mi-tabset}
#####  R Code
Conformamos en el código de R un dataframe que contiene el conjunto de criterios estadísticos a ser tenidos en cuenta a la hora de interpretar el test de Anova en la Temática 1) Visión General.
```{r eval=TRUE, echo=TRUE}

df_decision_cases_01_human <- df_decision_cases_init

df_decision_cases_01_human$"Decision"[1] <- "Cuando no se cumplen todos los requisitos del modelo, inmediatamente el análisis de Anova queda descartado.  
El descarte del análisis ocurre indistintamente de si no se cumple la normalidad de los residuos, la homogeneidad o ambos requisitos.  
Indistintamente de los valores estadísticos obtenidos en el test de Anova y en el test de Tukey, no es válido sacar conclusiones de ningún tipo. El análisis queda descartado en su totalidad.  
Debe buscarse otra herramienta estadística para analizar los datos de este dataset u otro camino estadístico.  
La herramienta más rápida de recomendar es el test de Kruskal-Wallis; pero un mejor análisis de las características del dataset tal vez permitan usar alguna herramienta de modelos lineales generales mixtos, modelos lineales generalizados, modelos lineales generalizados mixtos, o estadística exacta.  
El camino estadístico alternativo es realizar una transformación sobre la variable respuesta e intentar nuevamente un test de Anova a 1 Factor.  
Las opciones estadísticas son sugerencias de cuya factibilidad de uso debe ser verificada y justificada. La consulta la bibliografía de su área de estudio puede ser un buen paso determinar como han trabajado otros colegas ante esta situación.  

Continuar con las interpretaciones estadísticas sin el cumplimiento de los requisistos es un error grave de quien se inicia en la estadística, y una práctica común de quien no tiene el suficiente conocimiento estadístico para analizar de manera correcta a la realidad que intenta analizar.

Si a pesar de todas estas advertencias usted continua con el análisis, se expone severamente a que su trabajo sea duramente criticado por colegas, tribunales evaluativos y árbitros de publicaciones."

df_decision_cases_01_human$"Decision"[2] <- "Como se cumplen los requisitos del modelo it is valid to interpret the ANOVA p-value. The conclusions obtained from ANOVA are valid. The null hypothesis of ANOVA is not rejected. Según Anova, all means are statistically equal. La utilización de cualquier test comparaciones múltiple está supeditada al rechazo de la hipótesis nula de Anova, por lo tanto, queda inmediatamente descartada la utilización de cualquier test de comparaciones múltiple para analizar este dataset, ya sea el test de Tukey o cualquier otro.
La teoría estadística indica que el test de Anova es un test más potente que cualquier test de comparaciones múltiples.
Si bien en este la salida de Tukey coincide con la visión de Anova, en este contexto el test de Tukey no es válido. Nada debe decirse respecto al test de Tukey o cualquier test de comparaciones que se quiera utilizar.
En este caso afirmar que no hay diferencias por que Tukey no detectó grupos o afirmar que la interpretación es más fuerte por que anova y tukey coinciden es un error ya que en este caso no es válido sacar conclusiones de cualquier test de comparaciones múltiples.
If the operator needs to make a recommendation, they should recommend any factor level equally.
The fact of not rejecting the null hypothesis indicates that the mathematical differences between the means are due only to chance. Otra forma de decir esto es que de repetirse el experimento por segunda vez existe una gran probabilidad de que incluso ocurra que intercambio del valor de las medias entre los niveles del factor con la misma diferencia entre medias que ocurrió en la primera experiencia. Es un error común y grave insistir en que hay diferencias en alguna parte de los niveles del factor cuando se cumplen los requisitos del modelo y no se rechaza Ho de Anova.
La acción por parte del operador (usted) de insistir en diferencias cuando no se rechazó la hipótesis nula manifiesta una falta total de conocimientos estadísticos para interpretar los resultados obtenidos.
Como resumen, la teoría estadística indica que si se cumplen los requisitos del modelo el criterio de anova es un criterio de toma de decisiones superior a cualquier criterio que pueda determinar el operador. En este caso el criterio estadístico es el correcto en cualquier contexto."


df_decision_cases_01_human$"Decision"[3] <- "Como se cumplen los requisitos del modelo it is valid to interpret the ANOVA p-value. The conclusions obtained from ANOVA are valid. The null hypothesis of ANOVA is not rejected. Según Anova, all means are statistically equal. La utilización de cualquier test comparaciones múltiple está supeditada al rechazo de la hipótesis nula de Anova, por lo tanto, queda inmediatamente descartada la utilización de cualquier test de comparaciones múltiple para analizar este dataset, ya sea el test de Tukey o cualquier otro.
La teoría estadística indica que el test de Anova es un test más potente que cualquier test de comparaciones múltiples.
Si bien en este caso usted tiene una salida de Tukey que le indica grupos, el test de Tukey no es válido. Nada debe decirse respecto al test de Tukey o cualquier test de comparaciones que se quiera utilizar.
En este caso afirmar hay diferencias por que Tukey detectó grupos o afirmar que la interpretación del test de Tukey es más fuerte que Anova es un error muy grave ya que en este caso no es válido sacar conclusiones de ningún test de comparaciones múltiples.
If the operator needs to make a recommendation, they should recommend any factor level equally.
The fact of not rejecting the null hypothesis indicates that the mathematical differences between the means are due only to chance. Otra forma de decir esto es que de repetirse el experimento por segunda vez existe una gran probabilidad de que incluso ocurra que intercambio del valor de las medias entre los niveles del factor con la misma diferencia entre medias que ocurrió en la primera experiencia. Es un error común y grave insistir en que hay diferencias en alguna parte de los niveles del factor cuando se cumplen los requisitos del modelo y no se rechaza Ho de Anova.
La acción por parte del operador (usted) de insistir en diferencias entre los niveles del factor cuando no se rechazó la hipótesis nula de anova manifiesta una falta total de conocimientos estadísticos para interpretar los resultados obtenidos.
Como resumen, la teoría estadística indica que si se cumplen los requisitos del modelo el criterio de anova es un criterio de toma de decisiones superior a cualquier criterio que pueda determinar el operador. En este caso el criterio estadístico es el correcto en cualquier contexto."

df_decision_cases_01_human$"Decision"[4] <- "All ANOVA requirements are met. It is valid to interpret the ANOVA p-value. The conclusions obtained from ANOVA are valid. The null hypothesis of ANOVA is rejected. There is at least one different mean. The differences between the means are not only due to chance. At this point, ANOVA provides sufficient evidence to say that there are statistically significant differences between the factor level with the highest mean and the factor level with the lowest mean. By rejecting the null hypothesis of ANOVA, it is valid to interpret cualqueir test de comparaciones elegido, en este caso the Tukey test. In this case, the Tukey test does not distinguish statistical groups even though ANOVA rejected the null hypothesis. ANOVA is a more powerful test than Tukey, therefore it is affirmed that there are differences at least between the factor level with the largest mean and the factor level with the smallest one; and nothing can be said about the rest of the levels. It is a rare case, but it happens. Consider changing the multiple comparison test to another one, if appropriate, with the hope of being able to detect differences between factor levels with a different comparison test than Tukey.
La teoría estadística indica que las diferencias estadísticas entre niveles del factor pueden no ser diferencias significativas en reales en el área de estudio. El operador (usted) debe contextualizar la información estadística y decidir con buen criterio si las diferencias estadísticas son significativas en el contexto del trabajo. En caso de análisis el criterio del operador es superior al criterio estadístico."

df_decision_cases_01_human$"Decision"[5] <- "All ANOVA requirements are met. The conclusions obtained from ANOVA are valid. The null hypothesis of ANOVA is rejected. There is at least one different mean. The differences between the means are not only due to chance. At this point, ANOVA provides sufficient evidence to say that there are statistically significant differences between the factor level with the highest mean and the factor level with the lowest mean. By rejecting the null hypothesis of ANOVA, it is valid to interpret cualquier test de comparaciones múltiples, en esta oportunidad the Tukey test. In this case, the Tukey test distinguishes at least 2 statistical groups. The operator must observe the Tukey table and interpret the groups according to the letters assigned by the Tukey test, taking into account that Tukey can assign multiple letters to each factor level. The operator must make recommendations of factor levels taking into account the statistical framework and the original work framework. It is common to recommend the highest means or the lowest means according to the context of the data.
In the context of Tukey, factor levels that share at least 1 letter are statistically equal. It takes some time to get used to correctly interpreting the Tukey table. There are also other contexts where the lowest or highest means are not necessarily sought, such as specifically looking for which factor levels are statistically equal to a particular level. The Tukey test can also be applied in this context.
El operador (usted) debe contextualizar la información estadística y decidir con buen criterio si las diferencias estadísticas son significativas en el contexto del trabajo. En caso de análisis el criterio del operador es superior al criterio estadístico."


colnames(df_decision_cases_01_human)[ncol(df_decision_cases_01_human)] <- "General_Decision"
```



Podemos visualizar el caso seleccionado en la salida de R
```{r eval=TRUE, echo = TRUE}
df_selected_row_01_human <- df_decision_cases_01_human[row_match_index, ]
df_selected_row_01_human
```

#####  Full table cases
Detallamos en un formato más estético nuestra tabla con los 5 casos.  
La fila que corresponde a nuestro caso de análisis será en color.
```{r eval=TRUE, echo = FALSE}

my_df <- df_decision_cases_01_human
caption_text <- ""
col_align <- paste(rep("c", ncol(my_df)), collapse = "")
col_align[length(col_align)] <- "l"
  knitr::kable(my_df, format = "html", caption = caption_text, align = col_align) %>%
    # CAMBIO 2: Añadir position = "center" (centra la tabla completa en la página)
    kableExtra::kable_styling(bootstrap_options = c("striped", "hover", "condensed"),
                              full_width = FALSE,
                              position = "center",
                              font_size = 12) %>%
    kableExtra::column_spec(1, width = "8%") %>%    # Caso
    kableExtra::column_spec(2, width = "12%") %>%   # Homogeneidad
    kableExtra::column_spec(3, width = "10%") %>%   # ANOVA
    kableExtra::column_spec(4, width = "12%") %>%   # Tukey
    kableExtra::column_spec(5, width = "40%") %>%
    kableExtra::row_spec(
    row = row_match_index, # Usa el índice de la fila coincidente
    extra_css = "background-color: #FFFFCC;" # Color de fondo suave (amarillo claro)
    # Otros colores comunes: #E6F7FF (Azul claro), #E6FFE6 (Verde claro)
    )
 # ------------------------------------------------------------------
     # CÓDIGO A AGREGAR AL FINAL: Resalta la fila usando el índice calculado
    
# ------------------------------------------------------------------
```



#####  Selected case
Detallamos en un formato más estético solo el caso de análisis que se aplica a nuestros datos. 
```{r eval=TRUE, echo = FALSE}

# 4. Extraer la fila o el resultado deseado


my_df <-  df_selected_row_01_human
caption_text <- ""
col_align <- paste(rep("c", ncol(my_df)), collapse = "")
col_align[length(col_align)] <- "l"
  knitr::kable(my_df, format = "html", caption = caption_text, align = col_align) %>%
    # CAMBIO 2: Añadir position = "center" (centra la tabla completa en la página)
    kableExtra::kable_styling(bootstrap_options = c("striped", "hover", "condensed"),
                              full_width = FALSE,
                              position = "center",
                              font_size = 12) %>%
    kableExtra::column_spec(1, width = "8%") %>%    # Caso
    kableExtra::column_spec(2, width = "12%") %>%   # Homogeneidad
    kableExtra::column_spec(3, width = "10%") %>%   # ANOVA
    kableExtra::column_spec(4, width = "12%") %>%   # Tukey
    kableExtra::column_spec(5, width = "40%") %>%
    kableExtra::row_spec(
    row = 1, # Usa el índice de la fila coincidente
    extra_css = "background-color: #FFFFCC;" # Color de fondo suave (amarillo claro)
    # Otros colores comunes: #E6F7FF (Azul claro), #E6FFE6 (Verde claro)
    )
 # ------------------------------------------------------------------
     # CÓDIGO A AGREGAR AL FINAL: Resalta la fila usando el índice calculado
    
# ------------------------------------------------------------------
```


#####  ASA
<div style="background-color: #ff8c00; padding: 15px; border-radius: 5px;">
 Temática 1) Visión General
 
```{r eval=TRUE, echo = FALSE}
text_asa_01_human <- df_selected_row_01_human[1, ncol(df_selected_row_01_human)]
knitr::kable(df_decision_cases_init)%>%
    kableExtra::row_spec(
    row = 1, # Usa el índice de la fila coincidente
    extra_css = "background-color: #FFFFCC;" # Color de fondo suave (amarillo claro)
    # Otros colores comunes: #E6F7FF (Azul claro), #E6FFE6 (Verde claro)
    )
```
`r text_asa_01_human`
</div>
:::




### Asesor estadístico automatizado - Temática 2) Model Requeriments
Comenzamos detallando la visión general y los aspectos que comunmente tiene en mente un asesor estadístico humano a la hora de analizar el test de Anova. Lo que se otorga es una visión global resumina con detalles teóricos.

```{=html}
<script>
document.addEventListener('DOMContentLoaded', function(){
  // Seleccionar SOLO el panel con ID "mi-tabset111"
  const panel = document.getElementById('mi-tabset111');
  if (!panel) return;

  let tabs = panel.querySelectorAll('.nav a, .nav-link, [role="tab"]');

  // Activar SOLO la segunda pestaña de este panel
  if (tabs && tabs.length > 1) {
    tabs[3].click();
  }
});
</script>
```

::: {.panel-tabset #mi-tabset111}
##### R Code
Conformamos en el código de R un dataframe que contiene el conjunto de criterios estadísticos a ser tenidos en cuenta a la hora de interpretar el test de Anova. 
```{r eval=TRUE, echo=TRUE}

df_decision_cases_02_model_requeriments <- df_decision_cases_init

df_decision_cases_02_model_requeriments$"Decision"[1] <- "El test estadístico anova tiene 3 puntos sobre los que se basa para tomar decisiones: forma, dispersión y posición. La forma será la distribución normal de los residuos. La dispersión será varianzas homogéneas en los residuos y la posición será la media de la variable respuesta para uno de los niveles del factor. Garantizando que la forma y la dispersión de los residuos tienen cierta estructura en particular es que anova logra poner a prueba la posición en la variable respuesta. Por ello es requisito el cumplimiento de normalidad y homogeneidad de varianzas de los residuos, y solo con el cumplimiento de ambos requisitos de manerea simultánea es válido tomar la interpretación del las medias. Al no cumplir con todos los requisitos de manera simultánea debe ser descartado el análisis de anova indistintamente de los resultados que hubiera arrojado. No hay escapatoria y debe usted buscar otra herramienta estadística para interpretar estos resultados."

df_decision_cases_02_model_requeriments$"Decision"[2] <- "Como se cumplen los requisitos del modelo it is valid to interpret the ANOVA p-value. The conclusions obtained from ANOVA are valid. El hecho de que los residuos posean distribución normal y homogeneidad de varianzas en una característica que muchas veces es pasada por algo como un detalle menor. Lo que está usted viendo es una característica especial de datos. en muchos contextos pensar en el por qué los residuos de los niveles del factor son normales y homogéneos lleva a ideas que no se habían tenido en cuenta y enriquese muchísimo el trabajo que se está realizando. Si bien Anova es un test para las medias, Anova es abreviación 'Analisis de la Varianza' y no 'Analisis de las Medias', justamente por que através del análisis de la descomposición de varianzas en que logra decir algo de las medias. Pensar entonces en las varianzas de los residuos puede ser una idea interesante."


df_decision_cases_02_model_requeriments$"Decision"[3] <- df_decision_cases_02_model_requeriments$"Decision"[2]

df_decision_cases_02_model_requeriments$"Decision"[4] <- df_decision_cases_02_model_requeriments$"Decision"[2]

df_decision_cases_02_model_requeriments$"Decision"[5] <- df_decision_cases_02_model_requeriments$"Decision"[2]

colnames(df_decision_cases_02_model_requeriments)[ncol(df_decision_cases_02_model_requeriments)] <- "Requeriments_Decision"
```



Podemos visualizar el caso seleccionado en la salida de R
```{r eval=TRUE, echo = TRUE}
df_selected_row_02_model_requeriments <- df_decision_cases_02_model_requeriments[row_match_index, ]
df_selected_row_02_model_requeriments
```

##### Full table cases
Detallamos en un formato más estético nuestra tabla con los 5 casos.  
La fila que corresponde a nuestro caso de análisis será en color.
```{r eval=TRUE, echo = FALSE}

my_df <- df_decision_cases_02_model_requeriments
caption_text <- ""
col_align <- paste(rep("c", ncol(my_df)), collapse = "")
col_align[length(col_align)] <- "l"
  knitr::kable(my_df, format = "html", caption = caption_text, align = col_align) %>%
    # CAMBIO 2: Añadir position = "center" (centra la tabla completa en la página)
    kableExtra::kable_styling(bootstrap_options = c("striped", "hover", "condensed"),
                              full_width = FALSE,
                              position = "center",
                              font_size = 12) %>%
    kableExtra::column_spec(1, width = "8%") %>%    # Caso
    kableExtra::column_spec(2, width = "12%") %>%   # Homogeneidad
    kableExtra::column_spec(3, width = "10%") %>%   # ANOVA
    kableExtra::column_spec(4, width = "12%") %>%   # Tukey
    kableExtra::column_spec(5, width = "40%") %>%
    kableExtra::row_spec(
    row = row_match_index, # Usa el índice de la fila coincidente
    extra_css = "background-color: #FFFFCC;" # Color de fondo suave (amarillo claro)
    # Otros colores comunes: #E6F7FF (Azul claro), #E6FFE6 (Verde claro)
    )
 # ------------------------------------------------------------------
     # CÓDIGO A AGREGAR AL FINAL: Resalta la fila usando el índice calculado
    
# ------------------------------------------------------------------
```



##### Selected case
Detallamos en un formato más estético solo el caso de análisis que se aplica a nuestros datos. 
```{r eval=TRUE, echo = FALSE}

# 4. Extraer la fila o el resultado deseado


my_df <-  df_decision_cases_02_model_requeriments
caption_text <- ""
col_align <- paste(rep("c", ncol(my_df)), collapse = "")
col_align[length(col_align)] <- "l"
  knitr::kable(my_df, format = "html", caption = caption_text, align = col_align) %>%
    # CAMBIO 2: Añadir position = "center" (centra la tabla completa en la página)
    kableExtra::kable_styling(bootstrap_options = c("striped", "hover", "condensed"),
                              full_width = FALSE,
                              position = "center",
                              font_size = 12) %>%
    kableExtra::column_spec(1, width = "8%") %>%    # Caso
    kableExtra::column_spec(2, width = "12%") %>%   # Homogeneidad
    kableExtra::column_spec(3, width = "10%") %>%   # ANOVA
    kableExtra::column_spec(4, width = "12%") %>%   # Tukey
    kableExtra::column_spec(5, width = "40%") %>%
    kableExtra::row_spec(
    row = 1, # Usa el índice de la fila coincidente
    extra_css = "background-color: #FFFFCC;" # Color de fondo suave (amarillo claro)
    # Otros colores comunes: #E6F7FF (Azul claro), #E6FFE6 (Verde claro)
    )
 # ------------------------------------------------------------------
     # CÓDIGO A AGREGAR AL FINAL: Resalta la fila usando el índice calculado
    
# ------------------------------------------------------------------
```


##### ASA
<div style="background-color: #ff8c00; padding: 15px; border-radius: 5px;">
 Temática 2) Model Requeriments
 
```{r eval=TRUE, echo = FALSE}
text_asa_02_model_requeriments <- df_selected_row_02_model_requeriments[1, ncol(df_selected_row_02_model_requeriments)]
knitr::kable(df_decision_cases_selected)%>%
    kableExtra::row_spec(
    row = 1, # Usa el índice de la fila coincidente
    extra_css = "background-color: #FFFFCC;" # Color de fondo suave (amarillo claro)
    # Otros colores comunes: #E6F7FF (Azul claro), #E6FFE6 (Verde claro)
    )
```
`r text_asa_02_model_requeriments`
</div>
:::


### Asesor estadístico automatizado - Temàtica 3) Anova
Comenzamos detallando la visión general y los aspectos que comunmente tiene en mente un asesor estadístico humano a la hora de analizar el test de Anova. Lo que se otorga es una visión global resumina con detalles teóricos.

```{=html}
<script>
document.addEventListener('DOMContentLoaded', function(){
  // Seleccionar SOLO el panel con ID "mi-tabset222"
  const panel = document.getElementById('mi-tabset222');
  if (!panel) return;

  let tabs = panel.querySelectorAll('.nav a, .nav-link, [role="tab"]');

  // Activar SOLO la segunda pestaña de este panel
  if (tabs && tabs.length > 1) {
    tabs[3].click();
  }
});
</script>
```

::: {.panel-tabset #mi-tabset222}
#####  R Code
Conformamos en el código de R un dataframe que contiene el conjunto de criterios estadísticos a ser tenidos en cuenta a la hora de interpretar el test de Anova. 
```{r eval=TRUE, echo=TRUE}

df_decision_cases_03_anova <- df_decision_cases_init

df_decision_cases_03_anova$"Decision"[1] <- "No se cumplen los requisitos del modelo. El test estadístico anova tiene el requisito indispensable de normaldiad de los residuos y homogeneidad de varianzas. Ambos requisitos deben cumplirse de manera conjunta para que sea válido realizar conclusiones sobre los niveles del factor. Continuar con la interpretación estadística sin el cumplimiento de todos los requisitos es un error catastrófico. Los requisitos del modelo de Anova no son optativos y tampoco pueden asuminarse. Los requisitos del modelo son la base fundamental sobre la que se basa el test de Anova para tomar decisiones sobre las medias de los niveles del factor. En este caso, usted deberá buscar otra herramienta estadística para realizar el análisis de sus datos."

df_decision_cases_03_anova$"Decision"[2] <- "Como se cumplen los requisitos del modelo it is valid to interpret the ANOVA p-value. The conclusions obtained from ANOVA are valid. No se rechaza la hipótesis nula de Anova, por lo tanto no existen diferencias estadísticamente significativas entre los niveles del factor. Todos los niveles del factor son estadísticamente iguales. Si se necesita hacer una recomendación de niveles del factor, todos los nivelesl del factor deben ser recomendados por igual.
En el caso en que se cumplen los requisitos del modelo y no se rechaza la hipótesis nula las diferencias matemáticas observadas entre los niveles del factor es solo por azar. Esto quiere decir que si se repitiera la experiencia podría ocurrir uno de dos escenarios.
Escenario 1) Las diferencias entre las medias serían aún menor y también podría cambiar el orden de las medias si se ordenan los niveles del factor de mayor a menor.
Escenario 2) Las diferencias y las medias se mantienen pero las medias recaen en diferentes niveles del factor, cambiando entonces el orden de los niveles del factor si se ordenan los niveles del mayor a menor.

La teoría estadística es muy clara al respecto e indica que en caso de no rechazar la hipótesis nula entonces no existen diferencias, y si a ojo del operador si existen diferencias, prevalece el criterio estadístico por sobre el criterio del operador.
La acciones de insistir y argumentar en favor de conceptos como diferencias marginales o a ojímetro por parte del operador eso es un error estadístico grave que manifiesta un desconocimiento profundo de la estadística por parte del operador. 
"


df_decision_cases_03_anova$"Decision"[3] <- df_decision_cases_03_anova$"Decision"[2]


df_decision_cases_03_anova$"Decision"[4] <- "Como se cumplen los requisitos del modelo it is valid to interpret the ANOVA p-value. The conclusions obtained from ANOVA are valid. Se rechaza la hipótesis nula de Anova, por lo tanto existen diferencias estadísticamente significativas entre los niveles del factor. Las diferencias encontradas entre los niveles del factor no son solo por azar. Existen al menos dos grupos dentro de los niveles del factor. Anova garantiza que hay diferencias estadísticamente significativas entre el nivel del factor con la media menor y el nivel del factor con la media mayor. Solo eso. Nada dice del resto de los niveles del factor. La utilización de cualquier test de comparaciones está supeditada al rechazo de hipótesis nula de Anova. En este caso entonces es válido utilizar cualquier test de comparaciones múltiples para intentar visualizar una estructura de grupos entre los niveles del factor.

Si existe una discrepancia entre el test de Anova que indica que hay diferencias significativas y el operador que a su ojo no ve diferencias significativas, la teoría estadística es muy clara al respecto. Este marco en el cual anova indica que hay diferencias y el criterio del operador dice que no hay diferencias, el criterio del operador prevalece siempre y cuando sea argumento de manera correcta y profunda.

Como el test de anova es váido, y se ha rechazado la hipótesis nula de Anova, entonces es válido realizar cualquier test de comparaciones múltiple. En este caso 
"

df_decision_cases_03_anova$"Decision"[5] <- df_decision_cases_03_anova$"Decision"[4]

colnames(df_decision_cases_03_anova)[ncol(df_decision_cases_03_anova)] <- "Anova_Decision"
```



Podemos visualizar el caso seleccionado en la salida de R
```{r eval=TRUE, echo = TRUE}
df_selected_row_03_anova <- df_decision_cases_03_anova[row_match_index, ]
df_selected_row_03_anova
```

##### Full table cases
Detallamos en un formato más estético nuestra tabla con los 5 casos.  
La fila que corresponde a nuestro caso de análisis será en color.
```{r eval=TRUE, echo = FALSE}

my_df <- df_decision_cases_03_anova
caption_text <- ""
col_align <- paste(rep("c", ncol(my_df)), collapse = "")
col_align[length(col_align)] <- "l"
  knitr::kable(my_df, format = "html", caption = caption_text, align = col_align) %>%
    # CAMBIO 2: Añadir position = "center" (centra la tabla completa en la página)
    kableExtra::kable_styling(bootstrap_options = c("striped", "hover", "condensed"),
                              full_width = FALSE,
                              position = "center",
                              font_size = 12) %>%
    kableExtra::column_spec(1, width = "8%") %>%    # Caso
    kableExtra::column_spec(2, width = "12%") %>%   # Homogeneidad
    kableExtra::column_spec(3, width = "10%") %>%   # ANOVA
    kableExtra::column_spec(4, width = "12%") %>%   # Tukey
    kableExtra::column_spec(5, width = "40%") %>%
    kableExtra::row_spec(
    row = row_match_index, # Usa el índice de la fila coincidente
    extra_css = "background-color: #FFFFCC;" # Color de fondo suave (amarillo claro)
    # Otros colores comunes: #E6F7FF (Azul claro), #E6FFE6 (Verde claro)
    )
 # ------------------------------------------------------------------
     # CÓDIGO A AGREGAR AL FINAL: Resalta la fila usando el índice calculado
    
# ------------------------------------------------------------------
```



##### Selected case
Detallamos en un formato más estético solo el caso de análisis que se aplica a nuestros datos. 
```{r eval=TRUE, echo = FALSE}

# 4. Extraer la fila o el resultado deseado


my_df <-  df_selected_row_03_anova
caption_text <- ""
col_align <- paste(rep("c", ncol(my_df)), collapse = "")
col_align[length(col_align)] <- "l"
  knitr::kable(my_df, format = "html", caption = caption_text, align = col_align) %>%
    # CAMBIO 2: Añadir position = "center" (centra la tabla completa en la página)
    kableExtra::kable_styling(bootstrap_options = c("striped", "hover", "condensed"),
                              full_width = FALSE,
                              position = "center",
                              font_size = 12) %>%
    kableExtra::column_spec(1, width = "8%") %>%    # Caso
    kableExtra::column_spec(2, width = "12%") %>%   # Homogeneidad
    kableExtra::column_spec(3, width = "10%") %>%   # ANOVA
    kableExtra::column_spec(4, width = "12%") %>%   # Tukey
    kableExtra::column_spec(5, width = "40%") %>%
    kableExtra::row_spec(
    row = 1, # Usa el índice de la fila coincidente
    extra_css = "background-color: #FFFFCC;" # Color de fondo suave (amarillo claro)
    # Otros colores comunes: #E6F7FF (Azul claro), #E6FFE6 (Verde claro)
    )
 # ------------------------------------------------------------------
     # CÓDIGO A AGREGAR AL FINAL: Resalta la fila usando el índice calculado
    
# ------------------------------------------------------------------
```

##### ASA
<div style="background-color: #ff8c00; padding: 15px; border-radius: 5px;">
 Temática 3) Anova
 
```{r eval=TRUE, echo = FALSE}
text_asa_03_anova <- df_selected_row_03_anova[1, ncol(df_selected_row_03_anova)]
knitr::kable(df_decision_cases_selected)%>%
    kableExtra::row_spec(
    row = 1, # Usa el índice de la fila coincidente
    extra_css = "background-color: #FFFFCC;" # Color de fondo suave (amarillo claro)
    # Otros colores comunes: #E6F7FF (Azul claro), #E6FFE6 (Verde claro)
    )
```
`r text_asa_03_anova`
</div>
:::

### Asesor estadístico automatizado - Temática 4) Tukey
Comenzamos detallando la visión general y los aspectos que comunmente tiene en mente un asesor estadístico humano a la hora de analizar el test de Anova. Lo que se otorga es una visión global resumina con detalles teóricos.

```{=html}
<script>
document.addEventListener('DOMContentLoaded', function(){
  // Seleccionar SOLO el panel con ID "mi-tabset333"
  const panel = document.getElementById('mi-tabset333');
  if (!panel) return;

  let tabs = panel.querySelectorAll('.nav a, .nav-link, [role="tab"]');

  // Activar SOLO la segunda pestaña de este panel
  if (tabs && tabs.length > 1) {
    tabs[3].click();
  }
});
</script>
```

::: {.panel-tabset #mi-tabset333}
#####  R Code
Conformamos en el código de R un dataframe que contiene el conjunto de criterios estadísticos a ser tenidos en cuenta a la hora de interpretar el test de Anova. 
```{r eval=TRUE, echo=TRUE}

df_decision_cases_04_tukey <- df_decision_cases_init

df_decision_cases_04_tukey$"Decision"[1] <- "La utilización del test de Tukey está supeditada a la validez del modelo de Anova y al rechazo de la hipótesis de anova. Al no se cumplen los requisitos del modelo no es válido el test de Anova y por lo tanto tampoco es válido el test de Tukey. La información relacionada al test de Tukey debe ser descartada. Como ya se ha detallado antes, en este contexto continuar con la interpretación estadística es un error estadístico grave que manifiesta un desconocimiento total del criterios estadísticos para tomar decisiones."

df_decision_cases_04_tukey$"Decision"[2] <- "La utilización del test de Tukey está supeditada a la validez del modelo de Anova y al rechazo de la hipótesis de anova. Como el modelo es válido por cumplirse los requisitos y no se ha rechazado la hipótesis nula de anova, inmediatamente queda descartada la utilización no solo de Tukey sino de cualquier otro test de comparaciones múltiple. Todos los niveles del factor son estadísticamente iguales.
En este caso 2 Anova y Tukey son coherentes, en el sentido que ambos detallan que existe un solo grupo estadístico.  
La idea de afirmar que los resultados tienen más peso por que Anova y Tukey son coherentes en solo detectar un grupo, es un error ya desde un principio no es válido sacar ningún tipo de conclusiones o afirmaciones basado en cualquier test de comparaciones múltiple ya que la hipótesis nula de anova no ha sido rechazada.
"


df_decision_cases_04_tukey$"Decision"[3] <- "La utilización del test de Tukey está supeditada a la validez del modelo de Anova y al rechazo de la hipótesis de anova. Como el modelo es válido por cumplirse los requisitos y efectivamente  se ha rechazado la hipótesis nula de anova, inmediatamente queda habilitada la posibilidad de algún test de comparaciones múltiples. Para nustro caso es el test de Tukey. Al utilziar el test de Tukey recordar la importancia de detallar en la sentencia que utiliza al test de Tukey en R si hay o no descalance en repeticiones entre los niveles del factor.
En este caso 3 Anova y Tukey no son coherentes, en el sentido que Anova detalla que existen diferencias y Tukey no las encuentra.  
La teoría estadística indica que siempre prevalece Anova por sobr Tukey por ser Anova un test más pontente, por lo tanto existen diferencias estadísticamente significativas al menos entre el nivel del factor con la media menor y el nivel del factor con la media mayor; y nada podrá decirse del resto de los niveles del factor.
En algunos contextos esta situación dificulta la interprtación estadística o la posibilidad de recomendar niveles del factor. En tal caso, si es correcto, puede ayudar utilizar otro test de comparacicones múltiple que no sea Tukey, con la idea de encontrar efectivamente grupos entre los niveles del factor.
La discrepacia entre Anova y Tukey no es muy común, pero es posible. La mayoría de los casos se dan sobre todo cuando el operador insiste en ingresar al test estadístico sin cumplir los requisitos del modelo, o cuando el operador no detalla en la sentencia de R que hay un desbalance en los niveles del factor para que internamente Tukey aplique una corrección a su estimación. Pero aún cumpliendo los requisitos y detallando el argumento de manera correcta existe una chance de que esto pueda ocurrir ya que despsués de todo Anova y Tukey son dos test diferentes.
"

df_decision_cases_04_tukey$"Decision"[4] <- "La utilización del test de Tukey está supeditada a la validez del modelo de Anova y al rechazo de la hipótesis de anova. Como el modelo es válido por cumplirse los requisitos y no  se ha rechazado la hipótesis nula de anova, inmediatamente queda inhabilitada la posibilidad de aplicar algún test de comparaciones múltiples para acompañar al test de Anova. 
Indistintamente de los grupos estadísticos que pudieramos obtener en Tukey, el test de Tukey no debe ser tenido en cuenta para sacar conclusiones de ningún tipo.

La teoría estadística indica que el test de Anova es más potente que el test de Tukey y que hay una gerarquía
entre ellos a la hora de tomar decisiones: el test de Tukey solo es válido si se rechaza Ho de Anova.

Esta incoherencia entre el test de Anova y el test de Tukey suele verse cuando el operador es aún más incoherente 
y analiza Anova y Tukey sin cumplimiento requisitos del modelo. 

En este caso 4, todos los nivelesl del factor son estadísticamente iguales.
Y queda imposibilitada la utilización de cualquier test de comparaciones múltiple.
"


df_decision_cases_04_tukey$"Decision"[5] <- "La utilización del test de Tukey está supeditada a la validez del modelo de Anova y al rechazo de la hipótesis de anova. Como el modelo es válido por cumplirse los requisitos y efeectivametne se ha rechazado la hipótesis nula de anova, inmediatamente queda abierta la posibilidad de aplicar algún test de comparaciones múltiples para acompañar al test de Anova. 
En este caso 5 hay coherencia entre Anova y Tukey, en el sentido que ambos encuentran al menos dos grupos en los niveles del factor.

La garantía de Anova es que existen diferencias estadísticamente significativas entre el nivel del factor con la media menor y el nivel del factor con la media mayor.
Tukey entonces nos otorga una estructura de cómo son los grupos dentro de los niveles del factor.

El test de Tukey tiene la particularidad de que genera grupos intermedios. Cada nivel del factor puede pertenecer a más de un grupo estadístico. Lleva un tiempo acostumbarse a como interpretar el test de Tukey. Todos los nivelesl del factor que comparte al menos una letra de grupo son estadísticamente iguales entre si.
Es muy imporante contextualizar los grupos estadísticos en marco de referencia del trabajo.
"

colnames(df_decision_cases_04_tukey)[ncol(df_decision_cases_04_tukey)] <- "Tukey_Decision"
```



Podemos visualizar el caso seleccionado en la salida de R
```{r eval=TRUE, echo = TRUE}
df_selected_row_04_tukey <- df_decision_cases_04_tukey[row_match_index, ]
df_selected_row_04_tukey
```

#####  Full table cases
Detallamos en un formato más estético nuestra tabla con los 5 casos.  
La fila que corresponde a nuestro caso de análisis será en color.
```{r eval=TRUE, echo = FALSE}

my_df <- df_decision_cases_04_tukey
caption_text <- ""
col_align <- paste(rep("c", ncol(my_df)), collapse = "")
col_align[length(col_align)] <- "l"
  knitr::kable(my_df, format = "html", caption = caption_text, align = col_align) %>%
    # CAMBIO 2: Añadir position = "center" (centra la tabla completa en la página)
    kableExtra::kable_styling(bootstrap_options = c("striped", "hover", "condensed"),
                              full_width = FALSE,
                              position = "center",
                              font_size = 12) %>%
    kableExtra::column_spec(1, width = "8%") %>%    # Caso
    kableExtra::column_spec(2, width = "12%") %>%   # Homogeneidad
    kableExtra::column_spec(3, width = "10%") %>%   # ANOVA
    kableExtra::column_spec(4, width = "12%") %>%   # Tukey
    kableExtra::column_spec(5, width = "40%") %>%
    kableExtra::row_spec(
    row = row_match_index, # Usa el índice de la fila coincidente
    extra_css = "background-color: #FFFFCC;" # Color de fondo suave (amarillo claro)
    # Otros colores comunes: #E6F7FF (Azul claro), #E6FFE6 (Verde claro)
    )
 # ------------------------------------------------------------------
     # CÓDIGO A AGREGAR AL FINAL: Resalta la fila usando el índice calculado
    
# ------------------------------------------------------------------
```



#####  Selected case
Detallamos en un formato más estético solo el caso de análisis que se aplica a nuestros datos. 
```{r eval=TRUE, echo = FALSE}

# 4. Extraer la fila o el resultado deseado


my_df <-  df_selected_row_04_tukey
caption_text <- ""
col_align <- paste(rep("c", ncol(my_df)), collapse = "")
col_align[length(col_align)] <- "l"
  knitr::kable(my_df, format = "html", caption = caption_text, align = col_align) %>%
    # CAMBIO 2: Añadir position = "center" (centra la tabla completa en la página)
    kableExtra::kable_styling(bootstrap_options = c("striped", "hover", "condensed"),
                              full_width = FALSE,
                              position = "center",
                              font_size = 12) %>%
    kableExtra::column_spec(1, width = "8%") %>%    # Caso
    kableExtra::column_spec(2, width = "12%") %>%   # Homogeneidad
    kableExtra::column_spec(3, width = "10%") %>%   # ANOVA
    kableExtra::column_spec(4, width = "12%") %>%   # Tukey
    kableExtra::column_spec(5, width = "40%") %>%
    kableExtra::row_spec(
    row = 1, # Usa el índice de la fila coincidente
    extra_css = "background-color: #FFFFCC;" # Color de fondo suave (amarillo claro)
    # Otros colores comunes: #E6F7FF (Azul claro), #E6FFE6 (Verde claro)
    )
 # ------------------------------------------------------------------
     # CÓDIGO A AGREGAR AL FINAL: Resalta la fila usando el índice calculado
    
# ------------------------------------------------------------------
```


##### ASA
<div style="background-color: #ff8c00; padding: 15px; border-radius: 5px;">
 Temática 4) Tukey
 
```{r eval=TRUE, echo = FALSE}
text_asa_04_tukey <- df_selected_row_04_tukey[1, ncol(df_selected_row_04_tukey)]
knitr::kable(df_decision_cases_selected)%>%
    kableExtra::row_spec(
    row = 1, # Usa el índice de la fila coincidente
    extra_css = "background-color: #FFFFCC;" # Color de fondo suave (amarillo claro)
    # Otros colores comunes: #E6F7FF (Azul claro), #E6FFE6 (Verde claro)
    )
```
`r text_asa_04_tukey`
</div>
:::

